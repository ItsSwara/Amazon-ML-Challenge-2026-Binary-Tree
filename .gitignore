# Data and generated outputs (never commit datasets)
data/raw/
dataset/
data/processed/
output/
artifacts/
logs/
*.tsv
# ...except the 10k-row mock sample shared with the team
!data/mock/*.tsv

# Machine-specific config (copy configs/paths.example.yaml to this name)
configs/paths.local.yaml

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# OS / secrets
.DS_Store
.env
